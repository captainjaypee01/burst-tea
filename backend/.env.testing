# Loaded when APP_ENV=testing (PHPUnit / `php artisan test`).
# Overrides `.env` so the test runner never uses your development database.
# Keep this file committed; do not put secrets here.

APP_ENV=testing

DB_CONNECTION=sqlite
DB_DATABASE=:memory:
DB_URL=
